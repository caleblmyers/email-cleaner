{
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n api,svelte -c blue,green \"python3 main.py\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "npm run build && python3 main.py"
  }
}
